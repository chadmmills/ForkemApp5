<!DOCTYPE html>
<html>
  <%= render "layouts/head" %>
  <body>
    <%= render "layouts/flash" %>
    <div class="content flex flex-column">
      <%= render(
        "layouts/meal_planner_nav",
        planner_nav: PlannerNav.for(controller_action, local_assigns[:mealbook])
      ) %>
      <main class="main flex flex-auto ht1">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
