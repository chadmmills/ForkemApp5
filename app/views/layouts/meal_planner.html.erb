<!DOCTYPE html>
<html>
  <%= render "layouts/head" %>
  <body>
    <%= render "layouts/flash" %>
    <div class="content flex flex-column">
      <%= render "layouts/meal_planner_nav", mealbook: mealbook%>
      <main class="main flex flex-auto">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
